pool:
  vmImage: 'Ubuntu-16.04'

steps:
  - task: AzureCLI@2
    inputs:
      azureSubscription: 'azureconnection2'
      scriptType: 'bash'
      scriptLocation: 'scriptPath'
      scriptPath: 'az --version'