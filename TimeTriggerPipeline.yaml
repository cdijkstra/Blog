pool:
  vmImage: 'Ubuntu-16.04'

steps:
  - task: AzureCLI@2
    inputs:
      azureSubscription: 'azureconnection'
      scriptType: 'bash'
      scriptLocation: 'scriptPath'
      scriptPath: 'az --version'